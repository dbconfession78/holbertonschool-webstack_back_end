#!/usr/bin/python3
from api.v1.views import app_views
from os import getenv

app = Flask(__name__)
host = getenv('HBNB_API_HOST')
port = getenv('HBNB_API_PORT')
register_blueprint(app_views)


if __name__ == "__main__":
    app.start(host=host, port=port)
